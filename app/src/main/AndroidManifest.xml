<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.monthly_challenge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="Profile.MypointActivity"></activity>
        <activity android:name="Project.Team.TeamDetailPopupActivity" />
        <activity android:name="Profile.MyteamsActivity" />
        <activity android:name="Profile.MyprojectDetailActivity" />
        <activity
            android:name="Project.Team.TeamPopupActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name="Project.ProjectDetailActivity" />
        <activity android:name="Profile.MyprojectActivity" />
        <activity android:name="Profile.ProfileActivity" />
        <activity android:name="Login.BizSignupActivity" />
        <activity android:name="Login.SignupTypeActivity" />
        <activity android:name="Login.IndivSignupActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name="Login.LoginActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>